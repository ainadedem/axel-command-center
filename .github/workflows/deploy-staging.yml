name: Deploy to Staging

on:
  push:
    branches: [staging]
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy-ssr-reusable.yml
    with:
      branch: staging
      app_path: /var/www/axel-command-center-staging
      environment_name: staging
    secrets: inherit
