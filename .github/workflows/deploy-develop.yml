name: Deploy to Develop

on:
  push:
    branches: [develop]
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy-ssr-reusable.yml
    with:
      branch: develop
      app_path: /var/www/axel-command-center-develop
      environment_name: develop
    secrets: inherit
